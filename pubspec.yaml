name: flutter_bighustle
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.2



#saafsd.app@gmail.com





environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  dartz: ^0.10.1
  dio: ^5.4.3+1
  # flutter_stripe: ^10.1.1
  flutter_secure_storage: ^10.0.0
  get: 4.6.6
  image_picker: ^1.1.2
  path_provider: ^2.1.4
  socket_io_client: ^2.0.3+1
  video_player: ^2.8.6
  # Core Flutter API for querying products and handling purchases/subscriptions.
  in_app_purchase: ^3.1.5
  # Android platform bindings for Google Play Billing flows.
  in_app_purchase_android: ^0.4.0
  # iOS/macOS platform bindings for StoreKit purchase flows.
  in_app_purchase_storekit: ^0.3.10
  sign_in_with_apple: ^7.0.1
  webview_flutter: ^4.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/videos/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
